//This is where you define some of your applications metadata such as the name, version 
//and dependencies like so.

{
	"name": "MyApp",
	"version": "1.0.0",
	"dependencies": {
		"sax": "0.3.x",
		"nano": "*", 
		"request": ">0.2.0"
	}
}